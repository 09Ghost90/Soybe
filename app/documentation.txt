Teste Curl:

curl -v -X POST http://127.0.0.1:8001/inferencia \
  -H "Content-Type: application/json" \
  -d '{"model_name":"EfficientNetB0","images":["image1.jpg","image2.png"]}'

Estrutura previsata "model_name":"EfficientNetB0","images":["image1.jpg","image2.png"]

ESTRUTURA:

HTTP (FastAPI) -> ROUTE (recebe request)d -> SERVICE (regra de negócio / inferencia) -> MODEL (ML)
main.py -> Criar o FastAPI, Middlewares (CORS, etc) e ROTAS


Estrutura dos arquivos Inference:

services/inferencia.py -> Roda o modelo
routes